Tutorial Ubuntu 12.04 LTS

+ Atualizar o Servidor
	# apt-get update
	# apt-get upgrade

+ Apache + WSGI
	# apt-get install apache2 libapache2-mod-wsgi
	#  /etc/init.d/apache2 start

+ Python 2.7.3
	# apt-get python-dev python-setuptools

+ Git
	# apt-get install git

+ Pip
	# easy_install pip

+ Virtualenv
	# pip install virtualenv
	# virtualenv --system-site-packages DudeXP

+ Postgres
	# apt-get install postgresql-9.1 postgresql-server-dev-9.1
	# su postgres
	$ psql
	(bash) alter user postgres with password 'shift@dudexp';
	(bash) \q
	$ exit

+ PHPMyAdmin
	# apt-get install phppgadmin

	
